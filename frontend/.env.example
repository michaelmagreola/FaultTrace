# =============================================================================
# FaultTrace frontend — copy to frontend/.env if you need a custom API URL
#   copy .env.example .env
# =============================================================================

# Leave empty to use the Vite proxy (recommended for local dev).
# Set when the UI is hosted separately from the API.
VITE_API_BASE=
