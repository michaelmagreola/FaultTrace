# FaultTrace environment templates live next to each app:
#
#   backend/.env.example  → copy to backend/.env
#   frontend/.env.example → copy to frontend/.env (optional)
#
# Never commit real .env files. See README.md → Environment variables.
