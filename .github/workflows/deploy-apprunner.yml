name: Deploy FaultTrace to App Runner

# Disabled: App Runner CREATE_FAILED (health/boot). Use deploy-ecs-express.yml (RouteIQ path).
on:
  workflow_dispatch:

env:
  AWS_REGION: us-east-1
  ECR_REPOSITORY: faulttrace
  APP_RUNNER_SERVICE: faulttrace

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Skip
        run: |
          echo "App Runner deploy is disabled. Use workflow 'Deploy to ECS Express Mode'."
          exit 1
